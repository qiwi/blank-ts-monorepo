{
  "name": "blank-ts-monorepo",
  "version": "0.0.0",
  "description": "Blank TS monorepo",
  "main": "target/es5/index.ts",
  "workspaces": [
    "packages/*"
  ],
  "private": true,
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "clean": "lerna clean --yes && lerna run clean",
    "lint": "lerna run lint --concurrency 3 --parallel --stream --no-prefix",
    "test": "npm-run-all -p -l lint test:unit test:depcheck test:depaudit",
    "test:unit": "jest --runInBand --detectOpenHandles --forceExit",
    "test:report": "yarn test && yarn coveralls:push",
    "test:concurrent": "lerna run test --concurrency 3 --parallel --stream --no-prefix && yarn coverage:merge",
    "test:depcheck": "lerna run test:depcheck --concurrency 3 --parallel --stream --no-prefix",
    "test:depaudit": "npx yarn-audit-fix --audit-level=moderate",
    "coverage:merge": "node scripts/js/coverage-merge.js",
    "coveralls:push": "cat ./coverage/lcov.info | coveralls || echo 'coveralls push failed :(' && exit 0",
    "build": "lerna run build --stream --concurrency 3 --parallel",
    "docs": "typedoc packages/**/src/main",
    "uglify": "lerna run uglify --stream --concurrency 3 --parallel",
    "postupdate": "yarn && yarn bootstrap && yarn test:depaudit && yarn clean && yarn build && yarn test"
  },
  "devDependencies": {
    "@types/jest": "^26.0.18",
    "@types/node": "^14.14.11",
    "coveralls": "^3.1.0",
    "find-git-root": "^1.0.4",
    "jest": "^26.6.3",
    "lerna": "^3.22.1",
    "snazzy": "^9.0.0"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/antongolub/blank-ts-monorepo.git"
  }
}
